技术栈：vue2 + uniapp的项目 

目录结构：pages 文件夹下按照功能模块进行分类，每个模块下按照页面进行分类

代码规范：函数组件、命名约定、错误边界、要求按照项目中的规则来

接口对接：接口对接按照接口.md中的接口进行对接，对接完成后进行测试，接口的错误和成功的返回状态都要对接

设计系统：颜色、间距、组件库都按照项目中的规则来 整体一致
要求：
1. 所有页面都使用uniapp的组件库
2. 所有页面都使用uniapp的样式
3. 所有页面都使用uniapp的配置

到时候会将代码打包成ios和android的包，所以需要考虑ios和android的适配



# 第一轮 生成组件接口和类型定义
# 第二轮 生成 UI 骨架（无逻辑）
# 第三轮 接入 API 和状态管理


